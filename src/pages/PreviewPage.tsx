import { Box, Typography } from '@mui/material';
import React from 'react';

export interface PreviewPageProps {}

export const PreviewPage: React.FC<PreviewPageProps> = () => {
	return (
		<Box>
			<Typography>PreviewPage</Typography>
		</Box>
	);
};
